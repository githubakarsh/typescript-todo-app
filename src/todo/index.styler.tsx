import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    root: {
      maxWidth: 900,
      marginTop: 20,
      marginLeft: 250,
      padding : 20,
    },
    subcard : {
      margin: 50,
    }
  });

  export default useStyles;
  