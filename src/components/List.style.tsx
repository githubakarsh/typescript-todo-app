import { makeStyles } from '@material-ui/core/styles';

const ListStyler = makeStyles((theme) => ({
    root: {
      width: '100%',
      maxWidth: 'auto',
      marginBottom: 20,
    },
  }));

  export default  ListStyler;
